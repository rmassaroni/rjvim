return {
    {dir = "~/projects/enchantify",
    config = function()
        -- local enchant = require("enchantify")
        -- vim.api.nvim_create_user_command("Enchantify", enchant.enchant_current_buffer, {})
    end
    }
}
